# EXERCÍCIO:
1)  Crie os arquivos .feature, Schema.json, Service.js e Steps.js para executar um cenário de login positivo e um negativo para a API SERVEREST. Os testes deverão possuir as seguintes validações:

- Status code
- Json schema
- Mensagem de sucesso ou erro
- Validação da existência da chave authorization no cenário positivo
